# Physical AI and Humanoid Robotics Textbook
# Environment Configuration Example
# Copy this file to `.env.local` and fill in your values for development
# DO NOT COMMIT ACTUAL SECRETS - .env.local is gitignored

# ============================================================================
# GOOGLE OAUTH 2.0 AUTHENTICATION
# ============================================================================
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Steps:
# 1. Create OAuth 2.0 Client ID (Web Application)
# 2. Add Authorized Redirect URIs:
#    - Local: http://localhost:3000/api/auth/callback/google
#    - Production: https://your-domain.vercel.app/api/auth/callback/google
# 3. Add Authorized JavaScript Origins:
#    - Local: http://localhost:3000
#    - Production: https://your-domain.vercel.app

GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# ============================================================================
# BETTER AUTH CONFIGURATION
# ============================================================================
# Generate a random string for production security
# Use: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`

BETTER_AUTH_SECRET=your-random-secret-key-change-this-in-production

# ============================================================================
# APPLICATION BASE URL
# ============================================================================
# Must match your Authorized JavaScript Origins in Google Cloud Console

BASE_URL=http://localhost:3000

# For production in Vercel:
# BASE_URL=https://physical-ai-and-humanoid-robotics-t-peach.vercel.app

# ============================================================================
# ALGOLIA SEARCH CONFIGURATION
# ============================================================================
# Get from Algolia Dashboard: https://dashboard.algolia.com
# Steps:
# 1. Create an Algolia account
# 2. Create a new application
# 3. Go to API Keys section
# 4. Get your Application ID and Search-only API Key

ALGOLIA_APP_ID=your_algolia_app_id
ALGOLIA_API_KEY=your_algolia_search_only_api_key
ALGOLIA_INDEX_NAME=physical_ai_humanoid_robotics

# Note: Never use your Admin API Key in client-side code
# Always use the Search-only API Key for frontend

# ============================================================================
# OPTIONAL: DOCUSAURUS BUILD CONFIGURATION
# ============================================================================
# These are optional and usually not needed unless you have custom build needs

# BUILD_ENV=production
# NODE_ENV=production

# ============================================================================
# LOCAL DEVELOPMENT NOTES
# ============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your actual values
# 3. Start dev server: npm run dev
# 4. Sign-In button will appear in top-right of navbar
# 5. Click to authenticate with Google

# ============================================================================
# PRODUCTION DEPLOYMENT (VERCEL)
# ============================================================================
# 1. Go to Vercel Project Settings > Environment Variables
# 2. Add all variables above with production values:
#    - GOOGLE_CLIENT_ID (production value)
#    - GOOGLE_CLIENT_SECRET (production value)
#    - BETTER_AUTH_SECRET (new random string)
#    - BASE_URL (your Vercel domain)
#    - ALGOLIA_APP_ID
#    - ALGOLIA_API_KEY
#    - ALGOLIA_INDEX_NAME
# 3. Trigger redeploy
# 4. Test sign-in on production domain

# ============================================================================
# TROUBLESHOOTING
# ============================================================================
# Issue: "GOOGLE_CLIENT_ID not configured"
# Solution: Check .env.local exists and has correct values
#
# Issue: OAuth redirect_uri_mismatch
# Solution: Ensure BASE_URL matches Google Cloud Console Authorized URIs
#
# Issue: Session not persisting
# Solution: Check browser allows cookies (not incognito mode)
#
# Issue: Algolia search not working
# Solution: Verify ALGOLIA_API_KEY is the Search-only key, not Admin key


