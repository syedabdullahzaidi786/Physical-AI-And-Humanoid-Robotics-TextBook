import React from 'react';

// SearchComponent has been replaced by Algolia DocSearch.
// This stub exists to avoid build errors if any pages still import it.
export default function SearchComponent(): React.ReactElement {
  return (
    <div style={{padding: 8, fontSize: 14}}>
      Search replaced by Algolia DocSearch. Use the site search in the header.
    </div>
  );
}
