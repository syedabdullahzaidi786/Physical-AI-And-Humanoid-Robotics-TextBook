# Example Algolia env file — safe to commit
# Copy this to `.env` and replace values for local development.

ALGOLIA_APP_ID=YOUR_APP_ID_HERE
ALGOLIA_API_KEY=YOUR_SEARCH_ONLY_API_KEY_HERE
ALGOLIA_INDEX_NAME=YOUR_INDEX_NAME_HERE

# Better Auth (Google) - copy these into your local `.env` or set in Vercel
# See AUTHENTICATION.md for exact instructions on Allowed Origins / Redirect URIs
BETTER_AUTH_CLIENT_ID=YOUR_BETTER_AUTH_CLIENT_ID_HERE
BETTER_AUTH_CLIENT_SECRET=YOUR_BETTER_AUTH_CLIENT_SECRET_HERE
# Better Auth endpoints (usually default values are fine)
BETTER_AUTH_AUTHORIZE_URL=https://api.better-auth.com/oauth/authorize
BETTER_AUTH_TOKEN_URL=https://api.better-auth.com/oauth/token
BASE_URL=https://physical-ai-and-humanoid-robotics-t-peach.vercel.app

# Alternative Google-specific env vars used by the socialProviders config
# These can be set to the same values as BETTER_AUTH_* if using Better Auth as a
# proxy, or set to Google OAuth credentials directly.
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET_HERE
